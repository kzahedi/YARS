#include "DataConstraintFactory.h"


void DataConstraintFactory::createXsd(XsdSpecification *spec)
{
  // XsdSequence *constraints = new XsdSequence(YARS_STRING_CONSTRAINTS_DEFINITION);
  // spec->add(constraints);
}

# LSan suppressions, two narrowly scoped categories:
#  1. Third-party leaks provably outside YARS code — each entry names the
#     library and the reason.
#  2. Annotated pre-existing YARS legacy startup allocations, tracked for
#     removal rather than fixed in place.
# Do NOT add entries without a comment naming the tracking reference.
# NOTE: these entries were enumerated 2026-07-06 from the full 13-config
# corpus AFTER fixing the SIGABRT-swallowing bug that had masked all
# unsuppressed leaks as PASS (see commit "fix(main): don't trap SIGABRT").

# --- Pre-existing YARS legacy startup allocations (leaked-by-design until
# --- freed-at-exit ownership lands; removal tracked by
# --- docs/superpowers/plans/2026-07-06-json-config-migration.md Stage 4
# --- for the Data* layer; physics-layer entries are unique_ptr candidates
# --- recorded in docs/planning/v0.8.7-open-points.md).
# The Xerces-C third-party entry and the SAX/XSD-generator entries
# (YarsXSDSaxHandler::startElement, createXsd, XsdSpecification,
# YarsXSDSaxParser::read) were retired here — the XML/Xerces layer that
# produced them is deleted wholesale by JSON Stage 3 (this commit).
leak:parseJsonConfig
# Physics-layer startup allocations:
leak:Composite::init
leak:yars::PID::PID
# Config data-model constructors (owning raw members freed never/legacy):
leak:DataCameraConfig::DataCameraConfig
leak:DataObject::DataObject
leak:DataGenericProximitySensor::DataGenericProximitySensor
leak:DataGenericLightDependentResistorSensor::DataGenericLightDependentResistorSensor
leak:DataComposite::DataComposite
leak:DataScreen::DataScreen
leak:DataScreens::DataScreens
leak:DataController::add
leak:DataController::setModule
leak:NoiseFactory::create
# Startup path setup (util layer):
leak:Directories::__setup
